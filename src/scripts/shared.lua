-- this table is populated from individual files to solve dependency loops
-- TODO: Turn GUI tables into metatables so you can call methods on them directly, removing the need for `shared`!
return {}
